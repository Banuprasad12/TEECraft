{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "^/(.*)$",
      "dest": "/client/$1",
      "check": true
    },
    {
      "src": "^/(.*)$",
      "dest": "/client/index.html"
    }
  ]
}
